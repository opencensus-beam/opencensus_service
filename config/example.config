[
  {opencensus, [{sampler, {oc_sampler_always, []}},
                {reporter, {oc_reporter_service, []}},

                {stat, [{exporters, [{oc_stat_exporter_stdout, []}]}]}]},

 {grpcbox, [{client, #{channels => [{default_channel, [{http, "localhost", 55678, []}], #{}}]}}]}
].
